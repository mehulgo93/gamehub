import { Toggle } from "./toggle";
import { Wrapper } from "./wrapper";

export const Sidebar = () => {
  //fetch followers
  // fetch users
  return (
    <Wrapper>
      <Toggle />
    </Wrapper>
  );
};
